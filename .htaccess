<IfModule mod_rewrite.c>
    RewriteEngine On

    # If the request is for the properties folder, serve from public
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
